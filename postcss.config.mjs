export default {
  plugins: {
    '@tailwindcss/postcss': {},  // Use Tailwind's v4 PostCSS plugin
    // autoprefixer: {},        // Not needed in v4 (Tailwind does this internally)&#8203;:contentReference[oaicite:5]{index=5}
  }
}
